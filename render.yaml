services:
  - type: web
    name: deepface-basico
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn App:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"